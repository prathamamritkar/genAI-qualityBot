# ── Vercel / Flask backend ────────────────────────────────────────────────────
Flask==3.1.3
flask-cors==6.0.2
python-dotenv==1.2.1

# ── Transcription & LLM APIs ──────────────────────────────────────────────────
groq==1.0.0
deepgram-sdk==6.0.0rc2
elevenlabs==2.36.1

# ── Document parsing ──────────────────────────────────────────────────────────
PyPDF2==3.0.1

# ── Data validation ───────────────────────────────────────────────────────────
pydantic==2.12.5
# ── HTTP Client (required for OpenRouter LLM fallback) ─────────────────────────
requests>=2.31.0
# ── HuggingFace Space client (competitive execution — optional) ───────────────
# Required for the HF Space ASR node. Vercel installs this automatically.
# If Vercel build fails due to this dep, comment it out and the HF node
# will gracefully disable itself (GRADIO_CLIENT_AVAILABLE = False).
gradio_client>=1.3.0
